# Copy these into Coolify → Environment Variables (Application).
# Do not commit real secrets. Rebuild after changing any NEXT_PUBLIC_* value.

NEXT_PUBLIC_AZURE_CLIENT_ID=
NEXT_PUBLIC_AZURE_TENANT_ID=
NEXT_PUBLIC_ALLOWED_EMAIL_DOMAINS=allumiax.com
NEXT_PUBLIC_AZURE_AUTHORITY=https://login.microsoftonline.com/organizations

DATA_DIR=/app/data

TIMESHEETS_API_TOKEN=
TIMESHEETS_ORGANIZATION_ID=
TIMESHEETS_TIMEZONE=America/Los_Angeles
# TIMESHEETS_API_BASE_URL=https://timesheets.allumiax.com/api/v1

# --- Azure AD (portal steps, not env vars) ---
# App registration → Authentication → Single-page application
# Add redirect URI exactly matching the Coolify public origin, e.g.:
#   https://utilization.your-domain.com
# MSAL uses window.location.origin (see src/auth/authConfig.ts).
